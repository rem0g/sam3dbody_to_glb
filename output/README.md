# output/

Converted `.glb` files land here when you run `python batch_convert.py`.

Open them in:

- A browser: <https://gltf-viewer.donmccurdy.com/>
- Blender: *File ▸ Import ▸ glTF 2.0*
- Three.js / Babylon.js: `GLTFLoader`

> `.glb` files here are ignored by git (see `.gitignore`).
